
name: add contributor

on: push


jobs:
  contributor:
    if: 1==2
    runs-on: ubuntu-latest
    steps:
      - uses: milvus-io/hero-bot@master
        with:
          token: ${{ secrets.MYTOKEN }}
          repos: 'milvus-io/milvus,milvus-io/www.milvus.io'
          targetRepo: 'sutcalag/testaction'
          
